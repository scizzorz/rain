#!/bin/sh
if [ $# -gt 0 ]; then
  subc="$1"
  shift
else
  subc="help"
fi

target=rain-"$subc"
path=$(which "$target");
if [ -x "$path" ]; then
  "$path" "$@"
else
  echo "Command not found: $target"
fi
