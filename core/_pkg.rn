link "core.c"
link "util.c"

link "arith/arith.c"
link "set/set.c"
link "table/table.c"

library "gc"
library "unwind"
library ":libgcc_s.so.1"

import env
import except

let print = foreign "rain_ext_print"(val)
let exit = foreign "rain_exit"(val)
let panic = foreign "rain_ext_throw"(val)
let length = foreign "rain_length"(val)
let type = foreign "rain_type"(val)
let to_str = foreign "rain_to_string"(val)
