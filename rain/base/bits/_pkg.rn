link "bits.c"


module = table {
  lsh = foreign "rain_ext_lsh"(val, amt),
  rsh = foreign "rain_ext_rsh"(val, amt),
  or  = foreign "rain_ext_bor"(lhs, rhs),
  and = foreign "rain_ext_band"(lhs, rhs),
  xor = foreign "rain_ext_bxor"(lhs, rhs),
  not = foreign "rain_ext_bnot"(val),
}
