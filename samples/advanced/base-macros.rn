import macros

let fudge = ["one", "two", "three"]
let node = {
  tag = "node",
  body = "body",
}

let main = func()
  @macros.print_all(fudge[0], fudge[1], fudge[2])
  @macros.print_all("----", node.tag, node.body)
