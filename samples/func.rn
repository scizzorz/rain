add = func(x, y) # a function that adds two numbers
  return x + y

mul = func(x, y) # a function that multiplies two numbers
  return x * y

apply = func(fn, x, y) # a function that applies two numbers to another function
  return fn(x, y)


main = func()
  print add(3, 4)
  print mul(3, 4)
  print "-----"
  print apply(add, 3, 4)
  print apply(mul, 3, 4)
