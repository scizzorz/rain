let main = func()
  let f = table # tables are the only composite data structure
  f.not_true = false
  f.one = 1
  f.ten = 10.0
  f.number = "one"

  print(f["one"]) # x.y is syntactic sugar for x["y"]
  print(f[f.number])

  print(f.oops) # will result in a null
