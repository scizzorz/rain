
/* We use rain as a library in this testing framework, therefore
   the data which is normally defined in rain files and linked to
   is not present.

   Here, we mock these variables
*/
box rain_args;
