import ./arithmetic
import ./conversion
import ./comparison
import ./iters

var main = func()
  var results = [
    arithmetic(),
    comparison(),
    conversion(),
    iters(),
  ]

  for res in results:values()
    if !res
      return res

module = main
